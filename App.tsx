import LoginRoute from "./src/Routes/LoginRoute";


export default function App() {

  return (
    //MainRoutes is called which contains tab navigation which includes all of the components
    //it also includes a stack navigator for the home screen and its image click functionality
    <LoginRoute />
  );
}
